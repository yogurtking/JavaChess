package chess;

public enum PieceType {
	//maybe this should just be type without color? we have color already via PlayerColor
	//agreed just actual type
	PAWN, ROOK, KNIGHT, BISHOP, QUEEN, KING,
}
